% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dpit_helper.R
\name{dpit-methods}
\alias{dpit-methods}
\alias{print.dpit}
\alias{residuals.dpit}
\alias{summary.dpit}
\alias{print.summary.dpit}
\alias{plot.dpit}
\title{Methods for DPIT residual objects}
\usage{
\method{print}{dpit}(x, ...)

\method{residuals}{dpit}(object, scale = c("normal", "uniform"), ...)

\method{summary}{dpit}(object, scale = c("normal", "uniform"), ...)

\method{print}{summary.dpit}(x, ...)

\method{plot}{dpit}(x, scale = c("normal", "uniform"), line_args = list(), ...)
}
\arguments{
\item{x}{A \code{dpit} object to be printed or plotted by \code{print.dpit()} or \code{plot.dpit()}, or a \code{summary.dpit} object to be printed by \code{print.summary.dpit()}.}

\item{...}{Additional arguments passed to or from methods. For \code{plot.dpit()},
graphical arguments are passed to \code{stats::qqplot()} for customizing the QQ
plot (e.g., \code{pch}, \code{col}, \code{cex}, \code{xlab}, \code{ylab}).}

\item{object}{A \code{dpit} object whose residuals are extracted by \code{residuals.dpit()} or summarized by \code{summary.dpit()}.}

\item{scale}{You can choose the scale of the residuals among \code{normal} and \code{uniform}.
The sample quantiles of the residuals are plotted against
the theoretical quantiles of a standard normal distribution under the normal scale,
and against the theoretical quantiles of a uniform (0,1) distribution under the uniform scale.
The default scale is \code{normal}.}

\item{line_args}{A named list of graphical parameters passed to
\code{graphics::abline()} to modify the reference (red) 45° line
in the QQ plot. If left empty, a default red dashed line is drawn.}
}
\value{
\code{residuals.dpit()} returns a numeric vector of DPIT residuals.
\code{summary.dpit()} returns an object of class \code{summary.dpit}.
The print and plot methods return their input invisibly.
}
\description{
Methods for printing, summarizing, plotting, and extracting values from
objects returned by \code{dpit()}, \code{dpit_2pm()}, and the distribution-specific
DPIT calculators.
The print method displays the fitted model call or calls, when available,
and the sample size.
The summary method reports quantiles, mean, and standard deviation for the
selected scale.
The residuals method extracts the DPIT residuals, and the plot method constructs a QQ plot of the DPIT residuals against its reference distribution.
}
